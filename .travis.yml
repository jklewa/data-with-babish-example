language: node_js
node_js:
  - "9"
dist: trusty
sudo: false

branches:
  only:
  - master

cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - ng lint
  - ng build --base-href https://jklewa.github.io/data-with-babish-ghpages/
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist/data-with-babish-ghpages
  on:
    branch: master
