import { HighlightIngredientDirective } from './highlight-ingredient.directive';

describe('HighlightIngredientDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightIngredientDirective();
    expect(directive).toBeTruthy();
  });
});
